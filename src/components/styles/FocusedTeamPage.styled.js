import styled from "styled-components";

const StyledFocusedTeamPage = styled.div`
  position: relative;
  top: 15vh;
  left: 50%;
  transform: translate(-50%);

  width: 70%;

  display: flex;
  justify-content: space-between;
  flex-direction: column;
`;
export default StyledFocusedTeamPage;
