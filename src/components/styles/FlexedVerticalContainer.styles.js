import styled from "styled-components";

const StyledFlexedVerticalContainer = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
`;

export default StyledFlexedVerticalContainer;
