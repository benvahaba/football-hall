import styled from "styled-components";

const StyledFlexedColumnContainer = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  gap: 1rem;
`;
export default StyledFlexedColumnContainer;
